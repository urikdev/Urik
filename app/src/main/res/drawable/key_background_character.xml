<ripple xmlns:android="http://schemas.android.com/apk/res/android"
    android:color="?android:attr/colorControlHighlight">
    <item android:id="@android:id/mask">
        <shape android:shape="rectangle">
            <solid android:color="@android:color/white" />
            <corners android:radius="@dimen/key_corner_radius" />
        </shape>
    </item>
    <item>
        <selector>
            <!-- Disabled state -->
            <item android:state_enabled="false">
                <layer-list>
                    <item>
                        <shape android:shape="rectangle">
                            <solid android:color="@color/key_background_disabled" />
                            <corners android:radius="@dimen/key_corner_radius" />
                        </shape>
                    </item>
                    <!-- Subtle shadow for disabled state -->
                    <item android:top="1dp" android:left="1dp">
                        <shape android:shape="rectangle">
                            <solid android:color="@color/key_shadow_disabled" />
                            <corners android:radius="@dimen/key_corner_radius" />
                        </shape>
                    </item>
                </layer-list>
            </item>

            <!-- Pressed state -->
            <item android:state_pressed="true">
                <layer-list>
                    <item>
                        <shape android:shape="rectangle">
                            <solid android:color="@color/key_background_character_pressed" />
                            <corners android:radius="@dimen/key_corner_radius" />
                            <stroke android:width="1dp" android:color="@color/key_border_pressed" />
                        </shape>
                    </item>
                </layer-list>
            </item>

            <!-- Focused state (for accessibility navigation) -->
            <item android:state_focused="true">
                <layer-list>
                    <item>
                        <shape android:shape="rectangle">
                            <solid android:color="@color/key_background_character" />
                            <corners android:radius="@dimen/key_corner_radius" />
                            <stroke android:width="2dp" android:color="@color/key_border_focused" />
                        </shape>
                    </item>
                    <!-- Focus indicator for screen readers -->
                    <item android:top="2dp" android:bottom="2dp" android:left="2dp" android:right="2dp">
                        <shape android:shape="rectangle">
                            <stroke android:width="1dp" android:color="@color/key_focus_indicator" />
                            <corners android:radius="@dimen/key_corner_radius_small" />
                        </shape>
                    </item>
                </layer-list>
            </item>

            <!-- Default state with elevation -->
            <item>
                <layer-list>
                    <!-- Shadow layer for depth -->
                    <item android:top="2dp" android:left="1dp">
                        <shape android:shape="rectangle">
                            <solid android:color="@color/key_shadow" />
                            <corners android:radius="@dimen/key_corner_radius" />
                        </shape>
                    </item>
                    <!-- Main key background -->
                    <item android:bottom="2dp" android:right="1dp">
                        <shape android:shape="rectangle">
                            <solid android:color="@color/key_background_character" />
                            <corners android:radius="@dimen/key_corner_radius" />
                            <stroke android:width="1dp" android:color="@color/key_border" />
                        </shape>
                    </item>
                </layer-list>
            </item>
        </selector>
    </item>
</ripple>