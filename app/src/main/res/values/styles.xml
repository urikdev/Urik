<resources>
    <style name="KeyTextAppearance">
        <item name="android:textSize">@dimen/key_text_size</item>
        <item name="android:textColor">@color/key_text_character</item>
        <item name="android:includeFontPadding">false</item>
        <item name="android:textAllCaps">false</item>
        <item name="android:gravity">center</item>
    </style>

    <style name="KeyTextAppearance.Action">
        <item name="android:textColor">@color/key_text_action</item>
    </style>
</resources>