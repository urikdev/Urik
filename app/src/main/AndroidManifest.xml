<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.VIBRATE" />

    <application
        android:name=".UrikApplication"
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:localeConfig="@xml/locales_config"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Urik"
        >

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.Urik.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".settings.SettingsActivity"
            android:exported="false"
            android:theme="@style/Theme.Urik.NoActionBar"
            android:label="@string/settings_title"
            android:windowSoftInputMode="adjustResize" />

        <activity
            android:name=".settings.DictionaryAttributionActivity"
            android:exported="false"
            android:theme="@style/Theme.Urik.NoActionBar"
            android:label="@string/licenses_title" />

        <activity
            android:name=".settings.AboutActivity"
            android:exported="false"
            android:theme="@style/Theme.Urik.NoActionBar"
            android:label="@string/about_title" />

        <activity
            android:name=".settings.theme.ThemePickerActivity"
            android:exported="false"
            android:theme="@style/Theme.Urik.NoActionBar"
            android:label="@string/theme_picker_title" />

        <activity
            android:name=".settings.OssLicensesActivity"
            android:exported="false"
            android:theme="@style/Theme.Urik.NoActionBar"
            android:label="@string/licenses_title" />

        <service
            android:name=".UrikInputMethodService"
            android:exported="true"
            android:label="@string/ime_name"
            android:permission="android.permission.BIND_INPUT_METHOD">
            <intent-filter>
                <action android:name="android.view.InputMethod" />
            </intent-filter>
            <meta-data
                android:name="android.view.im"
                android:resource="@xml/method" />
        </service>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
    </application>
</manifest>